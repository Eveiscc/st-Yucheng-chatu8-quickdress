# 玉成-智绘姬快速换装

“玉成”是 SillyTavern 第三方扩展“智绘姬”的辅助插件，用于集中查看角色服装、快速切换智绘姬当前激活的角色服装列表，并将明确的换装指令写入聊天输入框。

本插件不会修改智绘姬的源码，也不会替代智绘姬自身的角色与服装管理功能。使用前请先安装并启用智绘姬。

## 主要功能

- 提供悬浮按钮或回复栏上方按钮，快速打开换装面板。
- 按角色分列显示已收录的角色服装，勾选状态同步显示智绘姬当前激活的服装。
- 支持勾选多名角色、每名角色勾选多套服装。
- 点击“替换角色服装列表”，将当前勾选结果同步到智绘姬并保存。
- 点击“立即穿上”，向聊天输入框追加每名角色最近勾选的一套服装指令，不覆盖原有输入内容。
- 服装存在参考图时，可以通过图片图标查看预览。
- 删除模式只会从玉成面板移除未启用服装，不会删除智绘姬中的服装预设，也不会移除当前启用中的服装。
- 支持桌面端与手机端使用。

## 安装

### 使用 SillyTavern 扩展安装器

1. 先安装并启用智绘姬。
2. 打开 SillyTavern 的扩展管理页面。
3. 选择安装第三方扩展，填入：

```text
https://github.com/Eveisc/st-yucheng-chatu8-quick-dress
```

4. 完成安装后，刷新 SillyTavern 页面。

### 手动安装

将本仓库完整放入 SillyTavern 的第三方扩展目录：

```text
public/scripts/extensions/third-party/st-yucheng-chatu8-quick-dress
```

刷新 SillyTavern 页面即可。

## 使用方式

1. 在 SillyTavern 扩展设置中找到“玉成-智绘姬快速换装”。
2. 勾选“启用玉成”，并选择入口位置。
3. 点击“玉成”按钮打开换装面板。
4. 点击刷新按钮，同步智绘姬当前激活服装的勾选状态。
5. 按需要勾选服装：
   - 点击“替换角色服装列表”，将所有角色的勾选结果同步到智绘姬。
   - 点击“立即穿上”，将每名角色最近勾选的一套服装写入聊天输入框。

## 删除模式

点击面板顶部的垃圾桶图标可进入删除模式。

- 只能从玉成面板移除未启用服装。
- 当前仍在智绘姬中启用的服装不会出现在可移除列表中。
- 移除后，智绘姬中的原服装预设仍会保留。
- 以后如果需要重新收录，可先在智绘姬角色服装列表中再次启用对应服装，再回到玉成刷新。

## 说明

- 玉成依赖智绘姬，不能单独使用。
- 玉成记录的是快速换装候选列表和界面状态；智绘姬仍然是角色服装数据的来源。
- “立即穿上”的默认文本可以在插件设置中的“输入内容”里修改。请保留 `{{角色名}}` 和 `{{服装名}}`，否则无法自动替换为实际名称。
